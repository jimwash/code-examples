/*global define*/
define([
	'jquery',
	'backbone',
	'collections/rectangles',
	'common'
], function ($, Backbone, Todos, Common) {
	'use strict';

	var RectangleRouter = Backbone.Router.extend({
	});

	return RectangleRouter;
});
